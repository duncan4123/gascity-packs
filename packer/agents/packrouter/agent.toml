scope = "rig"
provider = "omp-packrouter"
wake_mode = "fresh"
formula = "mol-packer-route"
work_dir = "{{.RigRoot}}"
nudge = "Run gc hook; inspect the request from the rig root, choose target packs, create pack-scoped beads, and route them to the shared packsmith pool."
idle_timeout = "2h"
