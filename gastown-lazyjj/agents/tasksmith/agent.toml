scope = "rig"
provider = "codex-mini"
wake_mode = "resume"
work_dir = ".gc/workspaces/{{.Rig}}/tasksmith/{{.AgentBase}}"
nudge = "Create LazyJJ-aligned task beads from tutorial skills and stack-aware requirements."
idle_timeout = "2h"
min_active_sessions = 1
max_active_sessions = 1
