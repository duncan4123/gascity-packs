{{ define "pack-improvement" }}
This workspace is an instance of the pack, and the pack files are the thing being exercised right now. The polecat session is running the pack’s templates, formulas, and agent configs, and the defects we’re finding are meant to be fixed in the pack so future polecat workspaces behave correctly.
{{ end }}
